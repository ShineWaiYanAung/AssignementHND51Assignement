
import 'package:flutter/material.dart';

class SchoolIntro extends StatelessWidget {
  const SchoolIntro({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "School",
        style: TextStyle(
            color: Colors.black
        ),
      ),
    );
  }
}
