import 'package:flutter/material.dart';

class GradesStatus extends StatelessWidget {
  const GradesStatus({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text(
            "Grades",
            style: TextStyle(
            color: Colors.black
        ),
    ),
    );
  }
}
